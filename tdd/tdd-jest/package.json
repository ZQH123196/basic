{
  "name": "tdd-jest",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest",
    "testAuto": "jest --watchAll --verbose",
    "testCoverage": "jest --verbose --coverage"
  },
  "devDependencies": {
    "jest": "^27.2.4",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "@types/jest": "^27.0.2"
  }
}
